
#include<iostream>
#include<cstdio>
#include<cmath>

int main()
{
	for(;;)
	{
		int a,b,v,A,s,x,y ;

		cin >> a >> b >> v >> A >> s >> x >> y ;

		if( a==0 && b==0 && v==0 && A==0 && s==0 && x==0 && y==0 )
			break ;

		double cur_x = (double)a/2 ;
		double cur_y = (double)b/2 ;
		double remained_time = s ;

		while( remained_time > 0.0 )
		{

		}


	}

	return 0 ;
}